<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><title>IAO Admin - IRIS</title><link rel="stylesheet" href="{{ asset('assets/style.css') }}"></head><body><div class="topbar"><h1>IRIS Admin — International Affairs Office</h1><form action="{{ route('logout') }}" method="POST">@csrf<button type="submit" class="btn btn-ghost">Logout</button></form></div><div class="container">@if(session('msg'))<p class="success">{{ session('msg') }}</p>@endif<div class="actions-row"><a href="{{ route('admin.smart-upload') }}" class="btn btn-primary">📄 Smart Upload — Word / PDF / Excel / Image</a><a href="{{ route('dashboard') }}" class="btn btn-outline">View public dashboard →</a></div><div class="card"><h3>Upload Data</h3><p class="sub">Upload a CSV file for one of the data types below. The system will parse it and refresh the public dashboard automatically.</p><form action="{{ route('admin.upload') }}" method="POST" enctype="multipart/form-data">@csrf<label>Data type</label><select name="upload_type" required><option value="rankings">Rankings (by ranking body)</option><option value="colleges">College Contributions</option><option value="programs">Program Rankings</option></select><label>CSV File</label><input type="file" name="csv_file" accept=".csv" required><button type="submit" class="btn btn-primary btn-block">Upload &amp; Process</button></form><p class="hint">Expected CSV columns —<br><b>rankings:</b> ranking_body_short_name, year, global_rank, ph_rank, note<br><b>colleges:</b> name, short_code, contribution_percent, year<br><b>programs:</b> name, college_short_code, national_rank, score, movement, year<br>For anything with a per-category breakdown (SDG ranks, QS Stars categories, WURI award categories) or program accreditation data, use <a href="{{ route('admin.smart-upload') }}">Smart Upload</a> instead — those aren't part of this plain CSV template.</p></div><div class="card"><h3>Recent Uploads</h3><p class="sub">File type is the literal format of the uploaded file; Data type is what IRIS filed it under.</p><table><tr><th>File</th><th>File Type</th><th>Data Type</th><th>Rows</th><th>Uploaded</th></tr>@forelse($logs as $row)<tr><td>{{ $row->filename }}</td><td><span class="badge">{{ $row->file_type ? strtoupper($row->file_type) : '—' }}</span></td><td><span class="badge">{{ $row->upload_type }}</span></td><td>{{ $row->rows_inserted }}</td><td>{{ $row->uploaded_at }}</td></tr>@empty<tr><td colspan="5" class="sub">No uploads yet.</td></tr>@endforelse</table></div></div></body></html>
